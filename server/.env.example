# Server Configuration
PORT=4000
NODE_ENV=development
FRONTEND_URL=http://localhost:5173

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=easymath
DB_USER=postgres
DB_PASSWORD=postgres

# JWT Secrets (CHANGE IN PRODUCTION!)
JWT_ACCESS_SECRET=change-this-to-a-random-secret-string-in-production
JWT_REFRESH_SECRET=change-this-to-another-random-secret-string-in-production

# Security
# Generate strong random secrets for production:
# openssl rand -base64 64
